import { DashIfEmptyPipePipe } from './dash-if-empty-pipe.pipe';

describe('DashIfEmptyPipePipe', () => {
  it('create an instance', () => {
    const pipe = new DashIfEmptyPipePipe();
    expect(pipe).toBeTruthy();
  });
});
